{
  "name": "quota-dock",
  "version": "0.1.0",
  "description": "Desktop widget for model provider quota, balance, and usage tracking.",
  "main": "out/main/index.js",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "vue-tsc --noEmit && electron-vite build",
    "preview": "electron-vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "electron": "^34.0.0",
    "electron-vite": "^2.3.0",
    "typescript": "^5.7.2",
    "vue-tsc": "^2.2.0"
  }
}
